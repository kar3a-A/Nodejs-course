


const ClearAndComplete = ({clearComplete}) => {

  return (
    <>
        <div>
            <button 
              className="button"
              onClick={clearComplete}
            >Clear completed
            </button>
        </div>

    </>
  )
}

export default ClearAndComplete