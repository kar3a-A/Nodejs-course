<!DOCTYPE html>
<html lang="en">
<%- include('components/head.ejs') %>


<body>
    <%- include('components/navbar.ejs') %>
    <h1>This is home page</h1>
    <% if (blog.length) { %>
        <% blog.forEach(blog => { %>
            <h1><%= blog.name %></h1>
            <h1><%= blog.age %></h1>
        <% }) %>
    <% } else { %>
        <h3>No blog found</h3>
    <% } %>
    


</body>
</html>